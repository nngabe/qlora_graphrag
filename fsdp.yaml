# fsdp.yaml
#compute_environment: LOCAL_MACHINE
distributed_type: FSDP
#downcast_bf16: 'no'
#fsdp_config:
  #fsdp_auto_wrap_policy: TRANSFORMER_BASED_WRAP
  #fsdp_backward_prefetch: BACKWARD_PRE
  #fsdp_offload_params: true # Set to true to offload parameters to CPU
  #fsdp_cpu_ram_efficient_loading: true # Essential for efficient loading with QLoRA
  #fsdp_sharding_strategy: FULL_SHARD
  #fsdp_state_dict_type: SHARDED_STATE_DICT
  #fsdp_transformer_layer_cls_to_wrap: LlamaDecoderLayer # For Llama models
  #fsdp_use_orig_params: false # Set to false when using QLoRA
#machine_rank: 0
#main_training_function: main
#mixed_precision: bf16 # Use bf16 for mixed precision training
num_machines: 1
#rdzv_backend: static
#same_network: true
num_processes: 2
#use_cpu: false
